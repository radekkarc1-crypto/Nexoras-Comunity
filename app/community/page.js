"use client";
import {useEffect,useState} from "react";
import {getSupabaseBrowser} from "../../lib/supabase-browser";
export default function Community(){const [users,setUsers]=useState([]),[q,setQ]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{(async()=>{try{const s=getSupabaseBrowser();const{data,error}=await s.from("profiles").select("id,username,display_name,avatar_url,created_at").order("created_at",{ascending:false});if(error)throw error;setUsers(data||[])}catch(e){setError(e.message||"Nie udało się pobrać użytkowników.")}finally{setLoading(false)}})()},[]);const list=users.filter(u=>`${u.username||""} ${u.display_name||""}`.toLowerCase().includes(q.toLowerCase()));return <main className="shell"><header className="topbar"><div className="logo">NEXORAS <span>COMMUNITY</span></div><a className="profile" href="/">← Nexoras</a></header><section className="notesPage"><div className="kicker">🌐 NEXORAS COMMUNITY</div><h1>Użytkownicy</h1><p>Poznaj ludzi tworzących Nexorasa.</p><div className="notesToolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="🔎 Szukaj po nicku..."/></div>{loading?<p>Ładowanie użytkowników... 🛰️</p>:error?<p>⚠️ {error}</p>:<div className="userGrid">{list.map(u=><a className="userCard" href={`/profile/${u.username||u.id}`} key={u.id}><div className="avatar">{u.avatar_url?<img src={u.avatar_url} alt=""/>:"👤"}</div><div><b>{u.display_name||u.username||"Nexorian"}</b><small>@{u.username||"user"}</small><span>🟢 Aktywny w Nexoras Community</span></div></a>)}{!list.length&&<p>Nie znaleziono użytkownika. 👻</p>}</div>}</section></main>}
